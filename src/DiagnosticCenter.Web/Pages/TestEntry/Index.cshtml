@page
@model DiagnosticCenter.Web.Pages.TestEntry.IndexModel
@{
    ViewData["Title"] = "Test Entries";
}

<div class="container-fluid mt-4">
    <div class="row mb-3">
        <div class="col">
            <h2><i class="bi bi-journal-medical"></i> Test Entries</h2>
        </div>
        <div class="col-auto">
            <a asp-page="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Add New Test Entry
            </a>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <form method="get" class="mb-3">
                <div class="input-group">
                    <input type="text" name="searchTerm" value="@Model.SearchTerm"
                           class="form-control" placeholder="Search by patient name or bill number...">
                    <button type="submit" class="btn btn-outline-secondary">
                        <i class="bi bi-search"></i> Search
                    </button>
                    @if (!string.IsNullOrEmpty(Model.SearchTerm))
                    {
                        <a asp-page="Index" class="btn btn-outline-secondary">
                            <i class="bi bi-x-circle"></i> Clear
                        </a>
                    }
                </div>
            </form>

            @if (Model.TestEntries.Any())
            {
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead class="table-dark">
                            <tr>
                                <th>Bill No</th>
                                <th>Patient Name</th>
                                <th>Test</th>
                                <th>Total Amount</th>
                                <th>Paid</th>
                                <th>Due</th>
                                <th>Due Date</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.TestEntries)
                            {
                                <tr>
                                    <td>@item.BillNo</td>
                                    <td>@item.PatientName</td>
                                    <td>@(item.TestName ?? "N/A")</td>
                                    <td>@item.TotalAmount.ToString("C")</td>
                                    <td>@item.PaidAmount.ToString("C")</td>
                                    <td>
                                        @if (item.DueAmount > 0)
                                        {
                                            <span class="text-danger fw-bold">@item.DueAmount.ToString("C")</span>
                                        }
                                        else
                                        {
                                            <span class="text-success">Paid</span>
                                        }
                                    </td>
                                    <td>@item.DueDate.ToString("dd/MM/yyyy")</td>
                                    <td>
                                        @if (item.IsActive)
                                        {
                                            <span class="badge bg-success">Active</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">Inactive</span>
                                        }
                                    </td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a asp-page="Details" asp-route-id="@item.Id"
                                               class="btn btn-sm btn-info" title="Details">
                                                <i class="bi bi-eye"></i>
                                            </a>
                                            <a asp-page="Edit" asp-route-id="@item.Id"
                                               class="btn btn-sm btn-warning" title="Edit">
                                                <i class="bi bi-pencil"></i>
                                            </a>
                                            <a asp-page="Delete" asp-route-id="@item.Id"
                                               class="btn btn-sm btn-danger" title="Delete">
                                                <i class="bi bi-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="alert alert-info">
                    <i class="bi bi-info-circle"></i>
                    @if (!string.IsNullOrEmpty(Model.SearchTerm))
                    {
                        <text>No test entries found matching "@Model.SearchTerm".</text>
                    }
                    else
                    {
                        <text>No test entries found. Click "Add New Test Entry" to create one.</text>
                    }
                </div>
            }
        </div>
    </div>
</div>
