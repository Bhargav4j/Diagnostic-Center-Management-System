@page
@model DiagnosticCenter.Web.Pages.Payments.DetailsModel
@{
    ViewData["Title"] = "Payment Details";
}

<div class="container-fluid mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h4 class="mb-0"><i class="bi bi-eye"></i> Payment Details</h4>
                </div>
                <div class="card-body">
                    @if (Model.Payment != null)
                    {
                        <dl class="row">
                            <dt class="col-sm-3">Payment ID</dt>
                            <dd class="col-sm-9">@Model.Payment.Id</dd>

                            <dt class="col-sm-3">Bill Number</dt>
                            <dd class="col-sm-9">
                                <span class="badge bg-primary">@Model.Payment.BillNo</span>
                            </dd>

                            <dt class="col-sm-3">Payment Amount</dt>
                            <dd class="col-sm-9">
                                <strong class="text-success">@Model.Payment.Amount.ToString("C")</strong>
                            </dd>

                            <dt class="col-sm-3">Payment Date</dt>
                            <dd class="col-sm-9">@Model.Payment.PaymentDate.ToString("dd/MM/yyyy")</dd>

                            <dt class="col-sm-3">Patient Name</dt>
                            <dd class="col-sm-9">@(Model.Payment.PatientName ?? "N/A")</dd>

                            <dt class="col-sm-3">Test</dt>
                            <dd class="col-sm-9">@(Model.Payment.TestName ?? "N/A")</dd>

                            <dt class="col-sm-3">Status</dt>
                            <dd class="col-sm-9">
                                @if (Model.Payment.IsActive)
                                {
                                    <span class="badge bg-success">Active</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Inactive</span>
                                }
                            </dd>

                            <dt class="col-sm-3">Created Date</dt>
                            <dd class="col-sm-9">@Model.Payment.CreatedDate.ToString("dd/MM/yyyy HH:mm")</dd>

                            @if (Model.Payment.ModifiedDate.HasValue)
                            {
                                <dt class="col-sm-3">Modified Date</dt>
                                <dd class="col-sm-9">@Model.Payment.ModifiedDate.Value.ToString("dd/MM/yyyy HH:mm")</dd>
                            }

                            @if (Model.Payment.CreatedBy.HasValue)
                            {
                                <dt class="col-sm-3">Created By</dt>
                                <dd class="col-sm-9">User ID: @Model.Payment.CreatedBy</dd>
                            }

                            @if (Model.Payment.ModifiedBy.HasValue)
                            {
                                <dt class="col-sm-3">Modified By</dt>
                                <dd class="col-sm-9">User ID: @Model.Payment.ModifiedBy</dd>
                            }
                        </dl>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-page="Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Back to List
                            </a>
                            <a asp-page="Edit" asp-route-id="@Model.Payment.Id" class="btn btn-warning">
                                <i class="bi bi-pencil"></i> Edit
                            </a>
                            <a asp-page="Delete" asp-route-id="@Model.Payment.Id" class="btn btn-danger">
                                <i class="bi bi-trash"></i> Delete
                            </a>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-warning">
                            <i class="bi bi-exclamation-triangle"></i> Payment not found.
                        </div>
                        <a asp-page="Index" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Back to List
                        </a>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
