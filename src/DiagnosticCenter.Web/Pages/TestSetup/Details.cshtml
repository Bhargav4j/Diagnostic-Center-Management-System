@page "{id:int}"
@model DiagnosticCenter.Web.Pages.TestSetup.DetailsModel
@{
    ViewData["Title"] = "Test Setup Details";
}

<div class="container mt-4">
    <h2>Test Setup Details</h2>

    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="alert alert-danger">@Model.ErrorMessage</div>
    }

    @if (Model.TestSetup != null)
    {
        <div class="card">
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-3">ID</dt>
                    <dd class="col-sm-9">@Model.TestSetup.Id</dd>

                    <dt class="col-sm-3">Name</dt>
                    <dd class="col-sm-9">@Model.TestSetup.Name</dd>

                    <dt class="col-sm-3">Fee</dt>
                    <dd class="col-sm-9">@Model.TestSetup.Fee.ToString("C")</dd>

                    <dt class="col-sm-3">Type</dt>
                    <dd class="col-sm-9">@Model.TestSetup.TypeName</dd>

                    <dt class="col-sm-3">Status</dt>
                    <dd class="col-sm-9">
                        @if (Model.TestSetup.IsActive)
                        {
                            <span class="badge bg-success">Active</span>
                        }
                        else
                        {
                            <span class="badge bg-danger">Inactive</span>
                        }
                    </dd>

                    <dt class="col-sm-3">Created</dt>
                    <dd class="col-sm-9">@Model.TestSetup.CreatedDate.ToString("g")</dd>

                    @if (Model.TestSetup.ModifiedDate.HasValue)
                    {
                        <dt class="col-sm-3">Modified</dt>
                        <dd class="col-sm-9">@Model.TestSetup.ModifiedDate.Value.ToString("g")</dd>
                    }
                </dl>
            </div>
            <div class="card-footer">
                <a asp-page="./Edit" asp-route-id="@Model.TestSetup.Id" class="btn btn-primary">Edit</a>
                <a asp-page="./Index" class="btn btn-secondary">Back to List</a>
            </div>
        </div>
    }
</div>
